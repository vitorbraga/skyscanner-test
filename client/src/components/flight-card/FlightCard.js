import { Card } from '../sky-components';
import React from 'react';

const FlightCard = (props) => (
  <Card>
    <div>sadsasda</div>
    <span>sdaaaaaaaaaa</span>
  </Card>
);

export default FlightCard;
